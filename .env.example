# SMTP Configuration for Contact Form
# These are required for the contact form to work

# SMTP server hostname
SMTP_HOST=your-smtp-host.com

# SMTP port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP username (often your email address)
SMTP_USER=your-smtp-username

# SMTP password
SMTP_PASS=your-smtp-password

# Email address to receive contact form submissions
EMAIL_TO=your-email@example.com

# Google Sheets API Configuration for Newsletter Subscriptions
# See GOOGLE_SHEETS_SETUP.md for detailed setup instructions

# Service account email from Google Cloud Console
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Private key from service account JSON file (keep the \n characters)
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Your Google Sheet ID (from the spreadsheet URL)
GOOGLE_SHEET_ID=your_spreadsheet_id_here

# Optional: Newsletter export token for secure CSV downloads
NEWSLETTER_EXPORT_TOKEN=your-random-export-token-here

# Twenty CRM Configuration for Contact Form Integration
# Optional: If configured, contact form submissions will automatically create Person records in Twenty CRM
# Generate API key from Settings > Developers in your Twenty CRM instance

# Your Twenty CRM GraphQL API endpoint (e.g., https://your-workspace.twenty.com/graphql)
TWENTY_API_URL=https://your-workspace.twenty.com/graphql

# API key generated from Twenty CRM Settings > Developers
TWENTY_API_KEY=your_twenty_api_key_here

# Name of the custom object in Twenty CRM for quote requests (optional)
TWENTY_QUOTE_OBJECT_NAME=quoteRequest

# Admin Dashboard Configuration
# Password for accessing the manufacturer dashboard at /admin/quotes
ADMIN_PASSWORD=your-secure-admin-password

# Cloudflare Turnstile CAPTCHA (for login security)
# Optional: If configured, CAPTCHA will appear after 2 failed login attempts
# Get keys from: https://dash.cloudflare.com/ → Turnstile
TURNSTILE_SITE_KEY=your-turnstile-site-key
TURNSTILE_SECRET_KEY=your-turnstile-secret-key

# Note: All analytics and tracking is handled through Google Tag Manager (GTM)
# No environment variables are needed for Google Analytics or Google Ads

# Supabase Configuration
# Get these from: Supabase Dashboard → Project Settings → API
# Required for product catalog database functionality
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# OpenRouter Configuration (for AI Chatbot)
# Get API key from: https://openrouter.ai/keys
# Optional: Only required if using the AI chatbot feature
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Google Ads API Configuration (for Admin Dashboard Analytics)
# Setup guide: See GOOGLE_ADS_ANALYTICS.md in /planning folder
# 1. Create Google Cloud project and enable Google Ads API
# 2. Create OAuth 2.0 Client ID (Web Application type)
# 3. Get developer token from Google Ads API Center
# 4. Generate refresh token via OAuth2 flow

# OAuth2 Client ID from Google Cloud Console
GOOGLE_ADS_CLIENT_ID=your-client-id.apps.googleusercontent.com

# OAuth2 Client Secret from Google Cloud Console
GOOGLE_ADS_CLIENT_SECRET=your-client-secret

# Developer token from Google Ads API Center (requires approval)
GOOGLE_ADS_DEVELOPER_TOKEN=your-developer-token

# Refresh token generated via OAuth2 flow
GOOGLE_ADS_REFRESH_TOKEN=your-refresh-token

# Your Google Ads customer ID (10-digit number, no dashes)
GOOGLE_ADS_CUSTOMER_ID=1234567890

# Optional: For MCC (Manager) accounts - the login customer ID
# GOOGLE_ADS_LOGIN_CUSTOMER_ID=0987654321

# Vercel Cron Secret (for securing scheduled jobs)
# Generate a random string and set it in Vercel environment variables
CRON_SECRET=your-random-cron-secret

# Invoice Ninja Configuration (for Admin Dashboard Invoicing)
# Self-hosted Invoice Ninja v5 API integration
# Get your API token from: Settings > Account Management > API Tokens

# Your Invoice Ninja instance URL (without trailing slash)
INVOICE_NINJA_URL=https://your-invoiceninja-instance.com

# API token from Invoice Ninja
INVOICE_NINJA_API_TOKEN=your-invoice-ninja-api-token